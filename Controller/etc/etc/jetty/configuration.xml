<Configure id="Server" class="org.eclipse.jetty.server.Server">

	<!-- define the thread pool -->
	<Set name="ThreadPool">
		<New class="org.eclipse.jetty.util.thread.QueuedThreadPool">
			<Set name="minThreads">1</Set>
			<Set name="maxThreads">5</Set>
		</New>
	</Set>


	<!-- add a new SelectChannelConnector -->
	<Call name="addConnector">
		<Arg>
			<New class="org.eclipse.jetty.server.nio.SelectChannelConnector">
				<Set name="host">
					<SystemProperty name="jetty.host" />
				</Set>

				<Set name="port">
					<SystemProperty name="jetty.port" default="8081" />
				</Set>

				<Set name="maxIdleTime">300000</Set>
				<Set name="Acceptors">2</Set>
				<Set name="statsOn">false</Set>
				<Set name="confidentialPort">8443</Set>
				<Set name="lowResourcesConnections">20000</Set>
				<Set name="lowResourcesMaxIdleTime">5000</Set>
			</New>
		</Arg>
	</Call>
	
	<Call name="addConnector">
		<Arg>
			<New class="org.eclipse.jetty.server.nio.BlockingChannelConnector">
				<Set name="host">
					<SystemProperty name="127.0.0.1" />
				</Set>

				<Set name="port">
					<SystemProperty name="jetty.port" default="8079" />
				</Set>

				<Set name="maxIdleTime">300000</Set>
				<Set name="Acceptors">1</Set>
				<Set name="statsOn">false</Set>
			</New>
		</Arg>
	</Call>

	<!-- set main handler -->
	<Set name="handler">
		<!-- main handler is a handler collection -->
		<New id="Handlers" class="org.eclipse.jetty.server.handler.HandlerCollection">
			<Set name="handlers">

				<Array type="org.eclipse.jetty.server.Handler">
					<Item>
						<New id="RequestHandler" class="org.prot.controller.RequestHandler" />
					</Item>

<!-- 					<Item>
						<New id="ContextCollection"
							class="org.eclipse.jetty.server.handler.ContextHandlerCollection" />
					</Item> -->
				</Array>

			</Set>
		</New>
	</Set>

	<!-- add a new request handler -->


</Configure>